const request = require("supertest");
const app = require("../../app");
const jest = require("jest");
/**
 * Pruebas módulo clases.
 * entiendase '-->' por 'debería retornar'
 */
describe("Ver mis clases", () => {
  it("", () => {
    return request;
  });
});
